<?xml version="1.0" encoding="utf-8"?><feed xmlns="http://www.w3.org/2005/Atom" xml:lang="en"><generator uri="https://jekyllrb.com/" version="4.4.1">Jekyll</generator><link href="https://hphucngh.github.io/feed.xml" rel="self" type="application/atom+xml"/><link href="https://hphucngh.github.io/" rel="alternate" type="text/html" hreflang="en"/><updated>2026-07-28T10:02:41+00:00</updated><id>https://hphucngh.github.io/feed.xml</id><title type="html">Timoty Nguyen</title><subtitle>Perspectives on applying AI in software and daily life. </subtitle><entry><title type="html">KV Cache in production: the exact VRAM formula, and cutting it 50% with FP8</title><link href="https://hphucngh.github.io/synthesis/2026/kv-cache-deep-dive/" rel="alternate" type="text/html" title="KV Cache in production: the exact VRAM formula, and cutting it 50% with FP8"/><published>2026-05-07T00:00:00+00:00</published><updated>2026-05-07T00:00:00+00:00</updated><id>https://hphucngh.github.io/synthesis/2026/kv-cache-deep-dive</id><content type="html" xml:base="https://hphucngh.github.io/synthesis/2026/kv-cache-deep-dive/"><![CDATA[<div class="bilang-bar" role="group" aria-label="Reading language"> <span class="bilang-hint"><b>EN</b> by default — <b>hover</b> a paragraph to peek the Vietnamese, <b>click</b> to translate it inline. Dotted <a class="term" href="/notes/glossary/" data-en="Dotted terms — hover for the definition, click to open the full glossary." data-vi="Thuật ngữ gạch chấm — rê để xem định nghĩa, bấm để mở trang glossary.">terms</a> link to the glossary.</span> <button class="bilang-btn" id="bilangToggle" type="button" aria-pressed="false">Dịch cả bài → VI</button> </div> <p class="bi" data-vi="Gần đây mình debug một hệ thống LLM serving OOM ngay khi vài user thật dùng cùng lúc, dù benchmark solo-user trên cùng GPU chạy êm re. Thủ phạm gần như luôn là KV cache — phần bộ nhớ hiếm ai tính đến lúc size GPU, nhưng ở context dài và nhiều user thì nó lớn hơn cả weights model.">Recently I debugged an LLM serving box that went OOM the moment a few real users hit it at once — even though the solo-user benchmark on the same GPU had run perfectly smooth. The culprit is almost always the <a class="term" href="/notes/glossary/#kv-cache" data-en="Cached Key/Value tensors for already-processed tokens, so attention doesn't recompute them on every generation step." data-vi="Bộ nhớ đệm lưu Key/Value của các token đã xử lý, để attention không phải tính lại từ đầu mỗi bước.">KV cache</a> — memory almost nobody accounts for when sizing a GPU, yet at long context and many users it grows larger than the model weights themselves.</p> <blockquote class="block-tip"> <h5 id="tldr">TL;DR</h5> <p>KV cache scales linearly with <code class="language-plaintext highlighter-rouge">context × concurrent users</code>, and at production scale it is usually <strong>larger than the weights</strong>. The full formula is <code class="language-plaintext highlighter-rouge">VRAM = Weights + KV Cache + Overhead</code> — counting only weights is a recipe for OOM. The fastest win: quantize the KV cache to <strong>FP8</strong> (−50% memory, ~99% quality, one flag), and use a serving engine with PagedAttention (vLLM / SGLang) instead of static allocation.</p> </blockquote> <p><em>Long read — use the left table of contents to jump. Sources at the <a href="#further-reading--references">end</a>.</em></p> <hr/> <h2 class="bi" data-vi="Bối cảnh &amp; ràng buộc" id="context--constraints">Context &amp; constraints</h2> <p class="bi" data-vi="Bài này dành cho bạn nếu đang tự host / serve LLM inference — nếu chỉ gọi API, provider đã lo phần này, không cần đọc tiếp.">This post is for you if you <strong>self-host / serve LLM inference</strong>. If you only call an API, the provider already handles all of this — you can stop here.</p> <p class="bi" data-vi="Trước khi vào công thức, xác định rõ 4 ràng buộc sau — chúng quyết định con số cuối cùng khác nhau bao nhiêu lần:">Before the formula, pin down these four constraints — they change the final number by whole multiples:</p> <ul> <li> <p class="bi" data-vi="GPU bạn có: số lượng, loại (VRAM, băng thông) — 1× RTX 4090 24GB khác hẳn 8× H100 80GB."><strong>The GPU you have:</strong> count and type (VRAM, bandwidth) — 1× RTX 4090 24GB is nothing like 8× H100 80GB.</p> </li> <li> <p class="bi" data-vi="Concurrent users kỳ vọng: 1 dev test khác 20 user production. Cache nhân tuyến tính theo số này."><strong>Expected concurrent users:</strong> one dev testing is not twenty production users. Cache multiplies linearly with this.</p> </li> <li> <p class="bi" data-vi="Context length cần hỗ trợ: chat ngắn (4K) khác document processing (128K+). Đây là biến ảnh hưởng mạnh nhất."><strong>Context length to support:</strong> short chat (4K) is not document processing (128K+). This is the single strongest variable.</p> </li> <li> <p class="bi" data-vi="Serving engine đang/sẽ dùng: Ollama cho prototype, vLLM/SGLang cho production — engine sai thì tối ưu đúng cũng vô nghĩa."><strong>Serving engine:</strong> Ollama for prototyping, vLLM / SGLang for production — the wrong engine makes every other optimization pointless.</p> </li> </ul> <p class="bi" data-vi="Bài giả định bạn đã chọn được model (không so sánh model nào tốt hơn) — tập trung vào phần capacity planning mà hầu hết kế hoạch deploy bỏ sót.">The post assumes you have <strong>already picked a model</strong> (it does not compare which is best) — it focuses on the capacity planning that most deployment plans skip.</p> <h2 class="bi" data-vi="Công thức tính VRAM cho KV cache" id="the-vram-formula-for-the-kv-cache">The VRAM formula for the KV cache</h2> <p class="bi" data-vi="Mỗi token trong context phải lưu Key và Value cho mỗi layer, mỗi KV head — vì attention cần nhìn lại toàn bộ token trước đó ở mọi bước sinh, và nếu tính lại từ đầu mỗi bước thì chi phí tăng theo bình phương độ dài chuỗi. KV cache đổi 'tính lại' lấy 'lưu trữ' — cái giá là bộ nhớ, tăng tuyến tính theo token thay vì bình phương theo thời gian.">Every token in the context must store a Key and a Value for <strong>each layer</strong> and <strong>each KV head</strong> — because attention has to look back at every prior token on every generation step, and recomputing from scratch each step would cost quadratically in sequence length. The KV cache trades “recompute” for “storage” — the price is memory that grows linearly with tokens instead of quadratically with time.</p> <p class="bi" data-vi="Công thức cho 1 token:">Per-token formula:</p> \[\text{KV cache/token (bytes)} = 2 \times n_{\text{layers}} \times n_{\text{kv\_heads}} \times d_{\text{head}} \times \text{bytes}\] <table> <thead> <tr> <th>Component</th> <th>Meaning</th> </tr> </thead> <tbody> <tr> <td><code class="language-plaintext highlighter-rouge">2</code></td> <td>Both Key and Value</td> </tr> <tr> <td><code class="language-plaintext highlighter-rouge">n_layers</code></td> <td>Number of transformer layers (<code class="language-plaintext highlighter-rouge">config.json: num_hidden_layers</code>)</td> </tr> <tr> <td><code class="language-plaintext highlighter-rouge">n_kv_heads</code></td> <td>Number of KV heads (<code class="language-plaintext highlighter-rouge">config.json: num_key_value_heads</code>) — with GQA, far fewer than query heads</td> </tr> <tr> <td><code class="language-plaintext highlighter-rouge">d_head</code></td> <td><code class="language-plaintext highlighter-rouge">hidden_size / num_attention_heads</code></td> </tr> <tr> <td><code class="language-plaintext highlighter-rouge">bytes</code></td> <td>BF16 = 2, FP8 = 1, Q4 = 0.5</td> </tr> </tbody> </table> <p class="bi" data-vi="Nhân lên cho cả chuỗi và batch:">Scale it up over the whole sequence and batch:</p> \[\text{Total (bytes)} = \text{batch\_size} \times \text{seq\_len} \times 2 \times n_{\text{layers}} \times n_{\text{kv\_heads}} \times d_{\text{head}} \times \text{bytes}\] <blockquote class="block-warning"> <h5 id="a-common-hand-calc-trap">A common hand-calc trap</h5> <p>Many calculators online use the shortened form <code class="language-plaintext highlighter-rouge">... × hidden_size × ...</code> (treating <code class="language-plaintext highlighter-rouge">num_kv_heads × head_dim = hidden_size</code>) — that only holds for <strong>MHA</strong>. Modern models use GQA, so <code class="language-plaintext highlighter-rouge">num_kv_heads</code> is much smaller — the shortened form <strong>overstates</strong> the number. Always use the full formula.</p> </blockquote> <p class="bi" data-vi="Ví dụ tính tay — Llama 3.1 8B (32 layers, 8 KV heads, head_dim 128, BF16):"><strong>Hand-calc example — Llama 3.1 8B</strong> (32 layers, 8 KV heads, head_dim 128, BF16):</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>1 token = 2 × 32 × 8 × 128 × 2 = 131,072 bytes ≈ 0.000125 GB
</code></pre></div></div> <table> <thead> <tr> <th>Context</th> <th>1 user</th> <th>10 users</th> <th>50 users</th> </tr> </thead> <tbody> <tr> <td>4K</td> <td>0.5 GB</td> <td>5 GB</td> <td>25 GB</td> </tr> <tr> <td>32K</td> <td>4 GB</td> <td>40 GB</td> <td>200 GB</td> </tr> <tr> <td>128K</td> <td><strong>16 GB</strong></td> <td><strong>160 GB</strong></td> <td>800 GB</td> </tr> </tbody> </table> <p class="bi" data-vi="Weights FP16 của model này chỉ ~16GB — ở 128K context, 1 user thôi cache đã bằng weights. Ở 10 user, cache gấp 10× weights. Đây là con số hay bị bỏ sót nhất khi size GPU.">This model’s FP16 weights are only ~16GB — at 128K context, <strong>one user’s cache already equals the weights</strong>. At 10 users, cache is 10× the weights. This is the number most often dropped when sizing a GPU.</p> <p class="bi" data-vi="Llama 3.3 70B (80 layers, 8 KV heads, head_dim 128, BF16): 1 token=2×80×8×128×2 = 327.680 bytes ≈ 0.00031 GB."><strong>Llama 3.3 70B</strong> (80 layers, 8 KV heads, head_dim 128, BF16): <code class="language-plaintext highlighter-rouge">1 token = 2×80×8×128×2 = 327,680 bytes ≈ 0.00031 GB</code>.</p> <table> <thead> <tr> <th>Context</th> <th>1 user</th> <th>5 users</th> <th>10 users</th> <th>20 users</th> </tr> </thead> <tbody> <tr> <td>4K</td> <td>1.3 GB</td> <td>6.5 GB</td> <td>13 GB</td> <td>26 GB</td> </tr> <tr> <td>32K</td> <td>10.2 GB</td> <td>51 GB</td> <td>102 GB</td> <td>204 GB</td> </tr> <tr> <td>128K</td> <td><strong>40.6 GB</strong></td> <td>203 GB</td> <td><strong>406 GB</strong></td> <td>812 GB</td> </tr> </tbody> </table> <p class="bi" data-vi="Deployment thật: Llama 3.3 70B INT4, 10 user, context 32K:"><strong>A real deployment:</strong> Llama 3.3 70B INT4, 10 users, 32K context:</p> <table> <thead> <tr> <th>Component</th> <th>Value</th> </tr> </thead> <tbody> <tr> <td>Weights (INT4)</td> <td>37 GB</td> </tr> <tr> <td>KV cache (10 users × 32K, BF16)</td> <td>102 GB</td> </tr> <tr> <td>KV cache (10 users × 32K, <strong>FP8</strong>)</td> <td><strong>51 GB</strong></td> </tr> <tr> <td>Activations + overhead</td> <td>~5 GB</td> </tr> <tr> <td><strong>Total (BF16 cache)</strong></td> <td><strong>144 GB → needs 2× H100</strong></td> </tr> <tr> <td><strong>Total (FP8 cache)</strong></td> <td><strong>93 GB → 2× H100, with room to spare</strong></td> </tr> </tbody> </table> <p class="bi" data-vi="KV cache (102GB) ở đây gấp gần 3 lần weights (37GB). Chỉ riêng đổi BF16→FP8 tiết kiệm 51GB — đủ để là ranh giới giữa 2 và 3 GPU.">Here the KV cache (102GB) is <strong>nearly 3× the weights</strong> (37GB). Just switching BF16→FP8 saves 51GB — enough to be the line between 2 and 3 GPUs.</p> <h2 class="bi" data-vi="Kiến trúc attention quyết định cache to hay nhỏ" id="the-attention-architecture-decides-cache-size">The attention architecture decides cache size</h2> <p class="bi" data-vi="Đây là đòn bẩy lớn nhất nhưng bạn hiếm khi tự chỉnh — nó nằm trong thiết kế model, không phải config lúc deploy. Hiểu nó để chọn đúng model cho ràng buộc VRAM của bạn.">This is the biggest lever, yet one you rarely tune yourself — it is baked into the model design, not a deploy-time config. Understand it so you <strong>pick the right model</strong> for your VRAM budget.</p> <table> <thead> <tr> <th>Attention</th> <th>KV Heads (70B)</th> <th>Cache @ 128K, 1 user</th> <th>vs MHA</th> </tr> </thead> <tbody> <tr> <td><strong>MHA</strong> (classic)</td> <td>64</td> <td><strong>327 GB</strong></td> <td>Baseline</td> </tr> <tr> <td><strong>GQA</strong> (Llama 3.3, most 2025-2026)</td> <td>8</td> <td><strong>40.6 GB</strong></td> <td>8× smaller</td> </tr> <tr> <td><strong>MQA</strong> (extreme)</td> <td>1</td> <td><strong>5.1 GB</strong></td> <td>64× smaller</td> </tr> </tbody> </table> <p class="bi" data-vi="GQA giữ ~99% chất lượng MHA mà cache giảm 8× — lý do gần như mọi model 2025–2026 đều dùng GQA. MQA giảm mạnh hơn nữa nhưng chất lượng giảm đáng kể nên ít ai dùng thuần. Thế hệ mới hơn (DeepSeek V4, Kimi K2.6) dùng MLA — nén K, V xuống không gian latent trước khi cache, hiệu quả hơn GQA, đổi lại kiến trúc phức tạp hơn."><a class="term" href="/notes/glossary/#gqa" data-en="Grouped-Query Attention — several query heads share one group of KV heads, cutting the KV heads to cache roughly 8×." data-vi="Grouped-Query Attention: nhiều query head chia sẻ chung một nhóm KV head → giảm số KV head phải cache (~8×).">GQA</a> keeps ~99% of MHA quality while cutting cache 8× — the reason nearly every 2025–2026 model uses it. <a class="term" href="/notes/glossary/#mqa" data-en="Multi-Query Attention — a single KV head shared by all query heads; the smallest cache, but a noticeable quality drop." data-vi="Multi-Query Attention: chỉ 1 KV head cho tất cả query head → cache nhỏ nhất, nhưng chất lượng giảm.">MQA</a> shrinks further but drops quality noticeably, so pure MQA is rare. Newer generations (DeepSeek V4, Kimi K2.6) use <a class="term" href="/notes/glossary/#mla" data-en="Multi-head Latent Attention — compresses K, V into a latent space before caching; more efficient than GQA, more complex." data-vi="Multi-head Latent Attention: nén K, V xuống không gian latent trước khi cache — hiệu quả hơn GQA, kiến trúc phức tạp hơn.">MLA</a> — compressing K, V into a latent space before caching, more efficient than GQA at the cost of a more complex architecture.</p> <h2 class="bi" data-vi="Bốn đòn bẩy giảm cache, xếp theo ROI" id="four-levers-to-cut-cache-ranked-by-roi">Four levers to cut cache, ranked by ROI</h2> <h3 class="bi" data-vi="1 — Quantize KV cache (làm trước tiên, rẻ nhất)" id="1--quantize-the-kv-cache-do-this-first-cheapest">1 — Quantize the KV cache (do this first, cheapest)</h3> <table> <thead> <tr> <th>Precision</th> <th>70B, 10 users, 32K</th> <th>Cut</th> <th>Quality</th> </tr> </thead> <tbody> <tr> <td>BF16 (default)</td> <td>102 GB</td> <td>Baseline</td> <td>100%</td> </tr> <tr> <td>FP8</td> <td><strong>51 GB</strong></td> <td>−50%</td> <td>~99%</td> </tr> <tr> <td>Q4_0</td> <td><strong>26 GB</strong></td> <td>−75%</td> <td>~95%</td> </tr> </tbody> </table> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># llama.cpp</span>
llama-server <span class="nt">--model</span> model.gguf <span class="se">\</span>
  <span class="nt">--cache-type-k</span> q4_0 <span class="nt">--cache-type-v</span> q4_0 <span class="se">\</span>
  <span class="nt">--flash-attn</span> <span class="nt">--ctx-size</span> 32768
</code></pre></div></div> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># vLLM</span>
vllm serve model <span class="nt">--kv-cache-dtype</span> fp8
</code></pre></div></div> <p class="bi" data-vi="Một dòng flag, giảm nửa cache, chất lượng gần như không đổi. Nếu chỉ làm một tối ưu, làm cái này trước.">One flag, half the cache, quality nearly unchanged. If you do a single optimization, do this one.</p> <h3 class="bi" data-vi="2 — Serving engine: đừng để bộ nhớ nằm không" id="2--serving-engine-dont-leave-memory-idle">2 — Serving engine: don’t leave memory idle</h3> <p class="bi" data-vi="Cấp phát tĩnh (đặt trước toàn bộ max context) lãng phí vì 3 lý do: giữ chỗ context chưa dùng, cấp thừa vì không biết output dài bao nhiêu, và khoảng trống giữa các request cỡ khác nhau.">Static allocation (reserving the full max context up front) wastes memory for three reasons: it holds unused context, over-provisions because output length is unknown, and leaves gaps between differently-sized requests.</p> <p class="bi" data-vi="PagedAttention (vLLM) mượn ý tưởng paging của OS — chia cache thành block nhỏ (~16 token), cấp phát non-contiguous, trả block ngay khi request xong."><a class="term" href="/notes/glossary/#paged-attention" data-en="Allocates the KV cache in small non-contiguous blocks (~16 tokens), borrowing the OS paging idea; frees blocks the moment a request finishes. (vLLM)" data-vi="Cấp phát KV cache theo block nhỏ (~16 token), non-contiguous, mượn ý tưởng paging của hệ điều hành (vLLM).">PagedAttention</a> (vLLM) borrows the OS paging idea — it splits the cache into small blocks (~16 tokens), allocates them non-contiguously, and frees a block the moment a request finishes.</p> <blockquote class="block-tip"> <p>GPU utilization goes from ~24% to <strong>~98.5%</strong>, batch size grows <strong>2–4×</strong> on the same hardware.</p> </blockquote> <p class="bi" data-vi="Continuous batching đưa request mới vào slot trống ngay khi có request xong, thay vì chờ cả batch xong mới nhận request mới — GPU luôn bận."><a class="term" href="/notes/glossary/#continuous-batching" data-en="Slots a new request into a freed spot the instant one finishes, keeping the GPU busy instead of waiting for the whole batch." data-vi="Nạp request mới vào slot trống ngay khi có request xong, thay vì chờ cả batch xong.">Continuous batching</a> slots a new request into a freed spot the instant one finishes, instead of waiting for the whole batch — the GPU stays busy.</p> <p class="bi" data-vi="Prefix caching: nhiều request chung system prompt thì chỉ tính + lưu cache phần đó một lần, dùng chung:"><a class="term" href="/notes/glossary/#prefix-caching" data-en="Computes and caches a shared prefix (e.g. a system prompt) once, then reuses it across requests." data-vi="Nhiều request chung system prompt thì chỉ tính và cache phần đó một lần, dùng chung.">Prefix caching</a>: when many requests share a system prompt, that part is computed and cached <strong>once</strong> and shared:</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>No prefix caching:  10 users × (2K system + 8K user) = 100K tokens cached
With prefix caching: 1 × 2K (shared) + 10 × 8K user  = 82K tokens cached  (−18%)
</code></pre></div></div> <p class="bi" data-vi="Với system prompt/RAG context dài (10K+ token), mức tiết kiệm lên tới 40–60%.">With long system prompts / RAG context (10K+ tokens), the savings reach 40–60%.</p> <h3 class="bi" data-vi="3 — Sliding window &amp; token eviction (tùy tình huống, không nên bật mặc định)" id="3--sliding-window--token-eviction-situational-dont-enable-by-default">3 — Sliding window &amp; token eviction (situational, don’t enable by default)</h3> <p class="bi" data-vi="Sliding Window (Mistral): chỉ giữ N token gần nhất, cache cố định bất kể context dài bao nhiêu. Hợp cho chat (thông tin gần quan trọng hơn), không hợp cho document processing (mất context ở đầu tài liệu)."><a class="term" href="/notes/glossary/#sliding-window" data-en="Keeps only the last N tokens, giving a fixed cache regardless of context length; good for chat, bad for document processing." data-vi="Chỉ giữ N token gần nhất, cache cố định bất kể context dài bao nhiêu; hợp chat, không hợp document processing.">Sliding Window</a> (Mistral): keep only the last N tokens, giving a fixed cache regardless of how long the context is. Good for <strong>chat</strong> (recent info matters more), <strong>bad for document processing</strong> (loses context at the start of the document).</p> <p class="bi" data-vi="Token eviction: StreamingLLM (giữ attention sink đầu chuỗi + N token gần nhất), H2O (giữ token có attention score tích lũy cao), Dynamic Memory Compression (nén nhóm token). Cho context 'hiệu dụng' dài hơn cache vật lý — nhưng thêm độ phức tạp vận hành, chỉ đáng cân nhắc khi 2 đòn bẩy trên chưa đủ."><a class="term" href="/notes/glossary/#token-eviction" data-en="Drops tokens from the cache by a heuristic (attention sink, cumulative attention score, group compression) for an effective context longer than the physical cache." data-vi="Bỏ bớt token khỏi cache theo tiêu chí (attention sink, điểm attention tích lũy, nén nhóm token).">Token eviction</a>: StreamingLLM (keep an attention sink at the start + the last N tokens), H2O (keep tokens with high cumulative attention score), Dynamic Memory Compression (compress groups of tokens). These give an “effective” context longer than the physical cache — but add operational complexity, only worth it once the two levers above aren’t enough.</p> <h3 class="bi" data-vi="4 — Biết khi nào KV cache không phải bottleneck chính (MoE, speculative decoding)" id="4--know-when-the-kv-cache-isnt-the-main-bottleneck-moe-speculative-decoding">4 — Know when the KV cache isn’t the main bottleneck (MoE, speculative decoding)</h3> <p class="bi" data-vi="MoE (Kimi K2.6, DeepSeek V4, GLM-5.1): attention layers vẫn tạo cache bình thường, nhưng expert layers không tạo cache — đổi lại toàn bộ expert weights phải nằm VRAM, vì token nào cũng có thể route tới bất kỳ expert nào (không thể offload ra CPU rồi nạp theo nhu cầu — latency không chấp nhận được). Với MoE, cache chiếm tỷ lệ nhỏ hơn trong tổng VRAM vì weights đã quá lớn, nhưng vẫn scale theo user/context: Kimi K2.6 — weights INT4 ~500GB (chiếm đa số) + cache 10 user @128K (BF16) ~200–300GB → tổng ~700–800GB, cần 8×H100 hoặc 4–6×H200."><a class="term" href="/notes/glossary/#moe" data-en="Mixture-of-Experts — only a subset of experts is active per token, but all expert weights must stay in VRAM (any token can route to any expert)." data-vi="Mixture-of-Experts: chỉ một phần expert active mỗi token, nhưng toàn bộ expert weights phải nằm trong VRAM.">MoE</a> (Kimi K2.6, DeepSeek V4, GLM-5.1): attention layers still produce cache normally, but expert layers don’t — in exchange, <strong>all</strong> expert weights must sit in VRAM, because any token can route to any expert (you can’t offload to CPU and load on demand — the latency is unacceptable). With MoE, cache is a smaller share of total VRAM because the weights are already huge, but it still scales with users/context: Kimi K2.6 — INT4 weights ~500GB (the majority) + cache for 10 users @128K (BF16) ~200–300GB → ~700–800GB total, needing 8×H100 or 4–6×H200.</p> <p class="bi" data-vi="Speculative decoding: model nhỏ (draft) đoán trước, model lớn (verifier) kiểm tra song song — mỗi model cần cache riêng: Tổng KV cache=cache_draft + cache_main. Speedup 1.5–2.5× thường xứng đáng, nhưng nhớ cộng VRAM của cả hai khi planning — phần cache của draft model dễ bị quên."><a class="term" href="/notes/glossary/#speculative-decoding" data-en="A small draft model guesses ahead while a large verifier checks in parallel; each model needs its own KV cache." data-vi="Model nhỏ (draft) đoán trước, model lớn (verifier) kiểm tra song song — mỗi model cần cache riêng.">Speculative decoding</a>: a small draft model guesses ahead, a large verifier checks in parallel — each model needs its own cache: <code class="language-plaintext highlighter-rouge">Total KV cache = cache_draft + cache_main</code>. A 1.5–2.5× speedup is usually worth it, but remember to add both models’ VRAM when planning — the draft model’s cache is easy to forget.</p> <h2 class="bi" data-vi="Vì sao bandwidth quan trọng hơn TFLOPS" id="why-bandwidth-matters-more-than-tflops">Why bandwidth matters more than TFLOPS</h2> <p class="bi" data-vi="Inference có 2 pha ngược tính chất: prefill (xử lý input, song song, compute-bound) và decode (sinh output, tuần tự, memory-bound). Mỗi bước decode phải đọc toàn bộ weights + KV cache từ VRAM chỉ để tính 1 token:">Inference has two phases of opposite character: <a class="term" href="/notes/glossary/#prefill" data-en="The phase that processes the whole input in parallel; compute-bound." data-vi="Pha xử lý toàn bộ input, chạy song song, giới hạn bởi compute.">prefill</a> (process the input, parallel, compute-bound) and <a class="term" href="/notes/glossary/#decode" data-en="The phase that generates output one token at a time, sequentially; memory-bandwidth-bound." data-vi="Pha sinh output từng token, tuần tự, giới hạn bởi băng thông bộ nhớ.">decode</a> (generate the output, sequential, memory-bound). Every decode step must read <strong>all the weights + the KV cache</strong> from VRAM just to produce one token:</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Data read per decode step = Weights + KV cache
= 37 GB (70B INT4) + 10.2 GB (1 user, 32K, BF16) = 47.2 GB

On an H100 (3,350 GB/s): max ≈ 3,350 / 47.2 ≈ 71 tokens/s (theoretical)
</code></pre></div></div> <p class="bi" data-vi="H100 có 990 TFLOPS nhưng 1 token decode chỉ cần ~1 GFLOP — compute utilization &lt;0.1%. Bottleneck gần như hoàn toàn là đọc bộ nhớ. Hệ quả trực tiếp: quantize weights + KV cache tăng tốc trực tiếp (ít data phải đọc); batch &gt; 1 hiệu quả vì weights chỉ đọc 1 lần cho cả batch.">An H100 has 990 TFLOPS, but one decode token needs only ~1 GFLOP — compute utilization &lt;0.1%. The bottleneck is almost entirely memory reads. Direct consequence: quantizing weights + KV cache speeds things up directly (less data to read); batch &gt; 1 helps because weights are read once for the whole batch.</p> <h2 class="bi" data-vi="Cạm bẫy &amp; điều học được" id="traps--lessons">Traps &amp; lessons</h2> <p class="bi" data-vi="Bốn sai lầm khiến bạn OOM lúc 2 giờ sáng — điểm chung: ai đó tính VRAM chỉ bằng weights."><strong>Four mistakes that OOM you at 2 a.m.</strong> — the common thread: someone computed VRAM from weights alone.</p> <blockquote class="block-danger"> <h5 id="an-8b-model-only-needs-4gb-int4">“An 8B model only needs 4GB (INT4)”</h5> <p>True for weights. But +4GB cache (32K) +1GB overhead = ~9GB — won’t fit an 8GB GPU if the context is long.</p> </blockquote> <blockquote class="block-danger"> <h5 id="an-h100-80gb-runs-70b-int4-for-20-users">“An H100 80GB runs 70B INT4 for 20 users”</h5> <p>INT4 weights 37GB + KV cache 20 users × 8K BF16 = 52GB → <strong>89GB &gt; 80GB → OOM</strong>. Fix: FP8 cache → 26GB → 65GB total → fits.</p> </blockquote> <blockquote class="block-danger"> <h5 id="llama-4-scouts-10m-context-runs-on-1-h100">“Llama 4 Scout’s 10M context runs on 1× H100”</h5> <p>Weights ~58GB + KV cache 1 user × 10M BF16 ≈ <strong>3,100 GB</strong> → needs ~40× H100. In practice Scout is capped at ~128K–256K on one GPU.</p> </blockquote> <blockquote class="block-danger"> <h5 id="moe-only-needs-vram-for-the-active-params">“MoE only needs VRAM for the active params”</h5> <p>Wrong. All 1T of Kimi K2.6’s parameters must be in VRAM even though only 32B are active per token.</p> </blockquote> <p class="bi" data-vi="Những gì mình cân nhắc nhưng bỏ — kèm lý do (để chặn trước câu hỏi 'sao không làm X'):"><strong>What I considered but dropped — with reasons</strong> (to pre-empt the “why not X?” questions):</p> <ul> <li> <p class="bi" data-vi="MQA thay vì GQA — giảm cache 64× thay vì 8×, nhưng chất lượng giảm đáng kể trên task reasoning dài trong test nội bộ. Bỏ, trừ khi VRAM là ràng buộc cứng không thể vượt qua."><strong>MQA instead of GQA</strong> — cuts cache 64× instead of 8×, but quality dropped noticeably on long-reasoning tasks in internal tests. Dropped, unless VRAM is a hard, unbeatable constraint.</p> </li> <li> <p class="bi" data-vi="Sliding window bật mặc định — cache cố định nghe hấp dẫn, nhưng làm hỏng document QA khi câu hỏi tham chiếu đầu tài liệu. Chỉ bật cho endpoint chat thuần, tắt cho endpoint document processing."><strong>Sliding window on by default</strong> — a fixed cache sounds attractive, but it breaks document QA when the question references the start of the document. Only enable it for pure chat endpoints; turn it off for document processing.</p> </li> <li> <p class="bi" data-vi="Ollama cho production — cài nhanh, tốt cho prototype, nhưng không có PagedAttention/continuous batching thật nên throughput không đủ khi nhiều user vào cùng lúc. Chuyển sang vLLM ngay khi qua giai đoạn demo."><strong>Ollama for production</strong> — quick to install, great for prototyping, but no real PagedAttention / continuous batching, so throughput collapses under concurrent users. Move to vLLM as soon as you’re past the demo stage.</p> </li> <li> <p class="bi" data-vi="CPU offload để 'tiết kiệm GPU' — chạy 70B trên RAM hệ thống chỉ được ~1–3 token/giây, đủ để test, không đủ production. Bỏ, chấp nhận mua/thuê thêm GPU thay vì offload."><strong>CPU offload to “save GPU”</strong> — running 70B on system RAM gives only ~1–3 tokens/sec, enough to test but not to serve. Dropped; better to buy/rent more GPU than to offload.</p> </li> </ul> <p class="bi" data-vi="Cheat sheet tra nhanh (KV cache theo model, 1 user, BF16):"><strong>Quick cheat sheet</strong> (KV cache by model, 1 user, BF16):</p> <table> <thead> <tr> <th>Model</th> <th>Per token</th> <th>4K ctx</th> <th>32K ctx</th> <th>128K ctx</th> </tr> </thead> <tbody> <tr> <td>Llama 3.1 8B</td> <td>0.125 MB</td> <td>0.5 GB</td> <td>4 GB</td> <td><strong>16 GB</strong></td> </tr> <tr> <td>Phi-4 14B</td> <td>0.098 MB</td> <td>0.4 GB</td> <td>3.1 GB</td> <td>12.5 GB</td> </tr> <tr> <td>Llama 3.3 70B</td> <td>0.31 MB</td> <td>1.3 GB</td> <td>10.2 GB</td> <td><strong>40.6 GB</strong></td> </tr> <tr> <td>Llama 4 Scout 109B</td> <td>~0.31 MB</td> <td>1.3 GB</td> <td>10 GB</td> <td>40 GB</td> </tr> </tbody> </table> <p class="bi" data-vi="Nhân hệ số nhanh: users ×1/×5/×10/×20 tương ứng; precision BF16 ×1, FP8 ×0.5, Q4 ×0.25.">Quick multipliers: users ×1/×5/×10/×20; precision BF16 ×1, FP8 ×0.5, Q4 ×0.25.</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>VRAM ≈ Weights(precision) + KV_cache(ctx × users × cache_precision) + 10–20% overhead
</code></pre></div></div> <p class="bi" data-vi="Công cụ tính nhanh (Python):">A quick calculator (Python):</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">def</span> <span class="nf">kv_cache_gb</span><span class="p">(</span>
    <span class="n">num_layers</span><span class="p">:</span> <span class="nb">int</span><span class="p">,</span>
    <span class="n">num_kv_heads</span><span class="p">:</span> <span class="nb">int</span><span class="p">,</span>
    <span class="n">head_dim</span><span class="p">:</span> <span class="nb">int</span><span class="p">,</span>
    <span class="n">seq_len</span><span class="p">:</span> <span class="nb">int</span><span class="p">,</span>
    <span class="n">batch_size</span><span class="p">:</span> <span class="nb">int</span> <span class="o">=</span> <span class="mi">1</span><span class="p">,</span>
    <span class="n">precision_bytes</span><span class="p">:</span> <span class="nb">float</span> <span class="o">=</span> <span class="mf">2.0</span>  <span class="c1"># BF16=2, FP8=1, Q4=0.5
</span><span class="p">)</span> <span class="o">-&gt;</span> <span class="nb">float</span><span class="p">:</span>
    <span class="sh">"""</span><span class="s">KV cache size in GB.</span><span class="sh">"""</span>
    <span class="n">total_bytes</span> <span class="o">=</span> <span class="p">(</span>
        <span class="n">batch_size</span> <span class="o">*</span> <span class="n">seq_len</span> <span class="o">*</span> <span class="mi">2</span> <span class="o">*</span> <span class="n">num_layers</span>
        <span class="o">*</span> <span class="n">num_kv_heads</span> <span class="o">*</span> <span class="n">head_dim</span> <span class="o">*</span> <span class="n">precision_bytes</span>
    <span class="p">)</span>
    <span class="k">return</span> <span class="n">total_bytes</span> <span class="o">/</span> <span class="p">(</span><span class="mi">1024</span> <span class="o">**</span> <span class="mi">3</span><span class="p">)</span>

<span class="c1"># Llama 3.3 70B, 10 users, 32K, BF16 vs FP8
</span><span class="nf">print</span><span class="p">(</span><span class="sa">f</span><span class="sh">"</span><span class="si">{</span><span class="nf">kv_cache_gb</span><span class="p">(</span><span class="mi">80</span><span class="p">,</span> <span class="mi">8</span><span class="p">,</span> <span class="mi">128</span><span class="p">,</span> <span class="mi">32768</span><span class="p">,</span> <span class="mi">10</span><span class="p">,</span> <span class="mi">2</span><span class="p">)</span><span class="si">:</span><span class="p">.</span><span class="mi">1</span><span class="n">f</span><span class="si">}</span><span class="s"> GB</span><span class="sh">"</span><span class="p">)</span>  <span class="c1"># → 100.0 GB
</span><span class="nf">print</span><span class="p">(</span><span class="sa">f</span><span class="sh">"</span><span class="si">{</span><span class="nf">kv_cache_gb</span><span class="p">(</span><span class="mi">80</span><span class="p">,</span> <span class="mi">8</span><span class="p">,</span> <span class="mi">128</span><span class="p">,</span> <span class="mi">32768</span><span class="p">,</span> <span class="mi">10</span><span class="p">,</span> <span class="mi">1</span><span class="p">)</span><span class="si">:</span><span class="p">.</span><span class="mi">1</span><span class="n">f</span><span class="si">}</span><span class="s"> GB</span><span class="sh">"</span><span class="p">)</span>  <span class="c1"># → 50.0 GB
</span></code></pre></div></div> <p class="bi" data-vi="Hoặc dùng công cụ online: LMCache KV Cache Calculator · LocalLLM VRAM Calculator.">Or use an online tool: <a href="https://lmcache.ai/kv_cache_calculator.html">LMCache KV Cache Calculator</a> · <a href="https://localllm.in/blog/interactive-vram-calculator">LocalLLM VRAM Calculator</a>.</p> <h2 class="bi" data-vi="Kết" id="wrap-up">Wrap-up</h2> <p class="bi" data-vi="Công thức duy nhất cần nhớ: VRAM=Weights + KV Cache(context × users × precision) + 10–20% overhead. Chỉ tính weights là công thức chắc chắn OOM khi user thật vào.">The one formula to remember: <strong>VRAM = Weights + KV Cache(context × users × precision) + 10–20% overhead.</strong> Counting only weights is a guaranteed OOM when real users arrive.</p> <p class="bi" data-vi="Nếu chỉ làm một việc sau khi đọc bài này: quantize KV cache xuống FP8. Một flag, giảm nửa bộ nhớ, chất lượng gần như không đổi — ROI cao nhất trong mọi lựa chọn ở đây.">If you do one thing after reading this: <strong>quantize the KV cache to FP8</strong>. One flag, half the memory, quality nearly unchanged — the highest ROI of every option here.</p> <p class="bi" data-vi="Bước tiếp theo tuỳ tình huống: nếu còn đang cân nhắc có nên tự host hay dùng API, mình có bài riêng về bài toán kinh tế self-host vs API. Có số liệu mới hơn hoặc cách làm khác, rất mong bạn góp ý ở phần bình luận.">The next step depends on your situation: if you’re still weighing self-host vs API, I have a separate piece on the <a href="/synthesis/2026/self-hosted-llm-guide/">self-host vs API economics</a>. If you have newer numbers or a different approach, I’d love your input in the comments.</p> <h2 class="bi" data-vi="Đọc thêm và nguồn tham khảo" id="further-reading--references">Further reading &amp; references</h2> <ol> <li><a href="https://huggingface.co/blog/not-lain/kv-caching">Not Lain — KV Caching Explained (Hugging Face)</a></li> <li><a href="https://developer.nvidia.com/blog/mastering-llm-techniques-inference-optimization/">NVIDIA — Mastering LLM Techniques: Inference Optimization</a></li> <li><a href="https://www.spheron.network/blog/gpu-memory-requirements-llm/">Spheron — GPU Memory Requirements for LLMs</a></li> <li><a href="https://lyceum.technology/magazine/kv-cache-memory-calculation-llm/">Lyceum — KV Cache Memory Calculation for LLMs</a></li> <li><a href="https://dev.to/bytecalculators/the-math-behind-local-llms-how-to-calculate-exact-vram-requirements-before-you-crash-your-gpu-12n5">DEV.to — The Math Behind Local LLMs VRAM</a></li> <li><a href="https://lmcache.ai/kv_cache_calculator.html">LMCache — KV Cache Size Calculator</a></li> <li><a href="https://localllm.in/blog/llamacpp-vram-requirements-for-local-llms">LocalLLM — llama.cpp VRAM Requirements</a></li> <li><a href="https://insiderllm.com/guides/kv-cache-optimization-guide/">InsiderLLM — KV Cache Optimization Guide</a></li> <li><a href="https://llmgarage.ai/kv-cache-optimization/">LLM Garage — KV Cache Optimization</a></li> <li><a href="https://arxiv.org/abs/2309.06180">vLLM — Efficient Memory Management with PagedAttention (Paper)</a></li> <li><a href="https://arxiv.org/abs/2205.14135">FlashAttention (Paper)</a></li> <li><a href="https://arxiv.org/abs/2305.13245">GQA: Training Generalized Multi-Query Transformer Models (Paper)</a></li> <li><a href="https://huggingface.co/docs/transformers/main/en/generation_strategies#kv-caching">Hugging Face — Generation Strategies: KV Caching</a></li> <li><a href="https://medium.com/@joaolages/kv-caching-explained-276520203249">João Lages — Transformers KV Caching Explained (Medium)</a></li> <li><a href="https://neptune.ai/blog/transformers-key-value-caching">Neptune.ai — Transformers Key-Value Caching</a></li> </ol> <hr/> <p><em>Updated: 2026-07-23. Bilingual mode is an experiment — Vietnamese translations are attached per paragraph; refine them anytime by editing the <code class="language-plaintext highlighter-rouge">data-vi</code> attributes.</em></p> <script>
  (function () {
    var root = document.querySelector(".post-content");
    if (!root) return;

    // ── floating peek tooltip ──
    var tip = document.createElement("div");
    tip.id = "bi-tip";
    document.body.appendChild(tip);
    var tipRAF = null;

    function showTip(text, label, isTerm, x, y) {
      tip.innerHTML = '<span class="bi-tip-lab">' + label + "</span>" + text;
      tip.classList.toggle("is-term", !!isTerm);
      tip.classList.add("on");
      moveTip(x, y);
    }
    function moveTip(x, y) {
      if (tipRAF) cancelAnimationFrame(tipRAF);
      tipRAF = requestAnimationFrame(function () {
        var w = tip.offsetWidth, h = tip.offsetHeight;
        var left = Math.min(x + 16, window.innerWidth - w - 12);
        var top = y + 18;
        if (top + h > window.innerHeight - 12) top = y - h - 14;
        tip.style.left = Math.max(12, left) + "px";
        tip.style.top = Math.max(12, top) + "px";
      });
    }
    function hideTip() { tip.classList.remove("on"); }

    // ── bilingual paragraphs (.bi) + technical terms (.term) ──
    // Handlers are delegated on the root so they survive a paragraph being
    // swapped to Vietnamese and back.
    var paras = root.querySelectorAll(".bi[data-vi]");
    paras.forEach(function (el) { el.dataset.en = el.innerHTML; });

    var lastKey = null;
    root.addEventListener("mousemove", function (e) {
      var term = e.target.closest ? e.target.closest(".term") : null;
      var para = term ? null : (e.target.closest ? e.target.closest(".bi[data-vi]") : null);
      if (!term && (!para || para.classList.contains("is-vi"))) { lastKey = null; hideTip(); return; }
      var key = term ? "t:" + term.href : "p:" + (para.dataset.vi || "").slice(0, 24);
      if (key !== lastKey) {
        lastKey = key;
        if (term) {
          // a term shows both languages at once: English definition + Vietnamese.
          var body = term.dataset.en || "";
          if (term.dataset.vi) body += '<span class="bi-tip-vi">' + term.dataset.vi + "</span>";
          showTip(body, "Definition · Nghĩa", true, e.clientX, e.clientY);
        } else {
          showTip(para.dataset.vi, "Tiếng Việt · bấm để dịch", false, e.clientX, e.clientY);
        }
      } else {
        moveTip(e.clientX, e.clientY);
      }
    });
    root.addEventListener("mouseleave", function () { lastKey = null; hideTip(); });

    // click a paragraph to translate it inline; links & terms keep working
    root.addEventListener("click", function (e) {
      if (e.target.closest("a, .term")) return;
      var para = e.target.closest(".bi[data-vi]");
      if (!para) return;
      hideTip();
      var toVi = !para.classList.contains("is-vi");
      para.classList.toggle("is-vi", toVi);
      para.innerHTML = toVi ? para.dataset.vi : para.dataset.en;
    });

    // ── whole-page toggle ──
    var btn = document.getElementById("bilangToggle");
    var allVi = false;
    if (btn) {
      btn.addEventListener("click", function () {
        allVi = !allVi;
        hideTip();
        paras.forEach(function (el) {
          el.classList.toggle("is-vi", allVi);
          el.innerHTML = allVi ? el.dataset.vi : el.dataset.en;
        });
        btn.classList.toggle("on", allVi);
        btn.setAttribute("aria-pressed", allVi ? "true" : "false");
        btn.textContent = allVi ? "Back to EN ← English" : "Dịch cả bài → VI";
      });
    }
  })();
</script>]]></content><author><name></name></author><category term="AI"/><category term="LLM"/><category term="KV-cache"/><category term="inference"/><summary type="html"><![CDATA[The exact formula for KV-cache VRAM, the constraints to nail before you deploy, and the traps that OOM your box in production — with Llama / DeepSeek / Kimi 2026 numbers and vLLM / llama.cpp snippets. English by default; hover or click any paragraph for the Vietnamese.]]></summary></entry><entry><title type="html">Tự host LLM hay dùng API: quyết định bằng con số / Self-Host LLM vs. API: A Numbers-Based Decision</title><link href="https://hphucngh.github.io/synthesis/2026/self-hosted-llm-guide/" rel="alternate" type="text/html" title="Tự host LLM hay dùng API: quyết định bằng con số / Self-Host LLM vs. API: A Numbers-Based Decision"/><published>2026-05-05T22:24:00+00:00</published><updated>2026-05-05T22:24:00+00:00</updated><id>https://hphucngh.github.io/synthesis/2026/self-hosted-llm-guide</id><content type="html" xml:base="https://hphucngh.github.io/synthesis/2026/self-hosted-llm-guide/"><![CDATA[<p><em>Đọc bằng / Read in: <a href="#tiếng-việt">Tiếng Việt</a> · <a href="#english">English</a></em></p> <hr/> <h2 id="tiếng-việt">Tiếng Việt</h2> <p>Tháng 5/2026, khoảng cách benchmark giữa model open-weight tốt nhất (Kimi K2.6, SWE-bench 80.2%) và model đóng tốt nhất (Claude Opus 4.6, 80.8%) chỉ còn <strong>0.6%</strong>, trong khi giá API open-weight rẻ hơn 5–70 lần. Câu hỏi “có nên tự host LLM không?” đột nhiên rất thật — và mình nhận ra phần lớn team đang tính sai theo 1 trong 2 hướng: hoặc size GPU thiếu rồi OOM ngay khi user thật vào, hoặc tưởng self-host rẻ rồi cuối tháng trả nhiều hơn cả API.</p> <blockquote class="block-tip"> <h5 id="tldr">TL;DR</h5> <p><strong>95% trường hợp nên dùng API.</strong> Self-host chỉ đáng khi lưu lượng rất lớn (&gt;50M token/ngày) hoặc bị bắt buộc bởi data residency. VRAM thật = Weights + KV Cache × users + overhead — quên KV cache là deploy xong OOM. Điểm hoà vốn cao (~50–100M token/ngày so với API frontier đắt tiền), và <strong>chi phí ẩn</strong> (GPU idle, setup, bảo trì) mới là thứ giết ROI, không phải giá GPU.</p> </blockquote> <p><em>Nguồn tổng hợp: Onyx AI Leaderboard, model card chính thức, phân tích chi phí thực tế. Danh sách đầy đủ ở <a href="#nguồn-tham-khảo">cuối bài</a>.</em></p> <hr/> <h3 id="bối-cảnh--ràng-buộc">Bối cảnh &amp; ràng buộc</h3> <p>Bài này dành cho bạn nếu team/sản phẩm đang <strong>cân nhắc serve LLM cho production</strong>, đã chọn được model ứng viên, và đang phân vân tự host hay dùng API. Nếu chỉ đang thử nghiệm/prototype, câu trả lời gần như luôn là API — đọc phần Kết để xác nhận.</p> <p>Trước khi tính toán, xác định rõ 5 ràng buộc sau — chúng quyết định câu trả lời khác nhau bao nhiêu lần:</p> <ul> <li><strong>Lưu lượng dự kiến (token/ngày):</strong> biến quan trọng nhất, quyết định điểm hoà vốn. Dưới 10M/ngày gần như luôn nên dùng API.</li> <li><strong>Ngân sách:</strong> capex (mua GPU) hay opex (thuê cloud, trả theo tháng) — self-host cần cam kết vốn hoặc hợp đồng dài hạn.</li> <li><strong>Ràng buộc compliance/data residency:</strong> HIPAA (y tế Mỹ), SOC 2 (tài chính), air-gapped (quốc phòng), hoặc Nghị định 13/2023 của Việt Nam về bảo vệ dữ liệu cá nhân.</li> <li><strong>Năng lực MLOps của team:</strong> ai vận hành, tune, xử lý incident lúc 2 giờ sáng? Không có người này thì self-host là rủi ro vận hành, không phải tiết kiệm.</li> <li><strong>Deadline:</strong> self-host cần tối thiểu 1–4 tuần setup; API ship được trong ngày.</li> </ul> <h3 id="bức-tranh-thị-trường-2026-ngắn-gọn">Bức tranh thị trường 2026 (ngắn gọn)</h3> <p>Open-weight đầu 2026 thay đổi rất nhanh — ít nhất 4 bản nâng cấp lớn chỉ trong Q1–Q2 (Kimi K2.5→K2.6, DeepSeek V3.2→V4, GLM-5→5.1, Qwen 3.5→3.6). Nếu đọc một bài so sánh model không ghi ngày, thông tin có thể đã cũ.</p> <table> <thead> <tr> <th style="text-align: left">Model</th> <th style="text-align: right">Active/token</th> <th style="text-align: right">Context</th> <th style="text-align: left">License</th> <th style="text-align: right">SWE-bench</th> </tr> </thead> <tbody> <tr> <td style="text-align: left">DeepSeek V4 Pro</td> <td style="text-align: right">49B/1.600B</td> <td style="text-align: right"><strong>1M</strong></td> <td style="text-align: left">MIT</td> <td style="text-align: right"><strong>80.6%</strong></td> </tr> <tr> <td style="text-align: left">Kimi K2.6</td> <td style="text-align: right">32B/1.000B</td> <td style="text-align: right">262K</td> <td style="text-align: left">MIT+</td> <td style="text-align: right">80.2%</td> </tr> <tr> <td style="text-align: left">GLM-5.1</td> <td style="text-align: right">40B/744B</td> <td style="text-align: right">200K</td> <td style="text-align: left">MIT</td> <td style="text-align: right">—</td> </tr> <tr> <td style="text-align: left">Qwen 3.5</td> <td style="text-align: right">17B/397B</td> <td style="text-align: right">—</td> <td style="text-align: left">Apache</td> <td style="text-align: right">—</td> </tr> <tr> <td style="text-align: left">Llama 4 Scout</td> <td style="text-align: right">17B/109B</td> <td style="text-align: right"><strong>10M</strong></td> <td style="text-align: left">Llama 4</td> <td style="text-align: right">—</td> </tr> <tr> <td style="text-align: left">Claude Opus 4.6*</td> <td style="text-align: right">—</td> <td style="text-align: right">—</td> <td style="text-align: left">—</td> <td style="text-align: right">80.8%</td> </tr> </tbody> </table> <p><em>* Model đóng, tham chiếu.</em></p> <p>Ba điều rút ra: hầu hết model frontier là <strong>MoE</strong> (không thể so sánh chỉ bằng “số B” tham số); context window đang bùng nổ nhưng KV cache ngốn VRAM tuyến tính theo token nên production thực tế thường giới hạn 128K–256K; <strong>MIT license chiếm ưu thế</strong> — tự do thương mại hoá, fine-tune, redistribute. Benchmark cũng phụ thuộc harness đo (cùng model có thể lệch 10 điểm % tuỳ harness) — luôn test trên workload thật của bạn, không có “model tốt nhất chung chung”.</p> <h3 id="công-thức-vram-con-số-hay-bị-tính-sai">Công thức VRAM: con số hay bị tính sai</h3> \[\text{VRAM MIN} = \text{Weights} + \text{KV Cache} + \text{Activations} + \text{Overhead}\] <p>Weights là phần ai cũng tính đúng (FP16 = 2 bytes/param, INT4 = 0.5 bytes/param). <strong>KV cache</strong> mới là thủ phạm thầm lặng — mỗi user đồng thời cần cache riêng, và nó tăng tuyến tính theo <code class="language-plaintext highlighter-rouge">context × users</code>.</p> <p>Ví dụ Llama 3.3 70B (80 layers, 8 KV heads, BF16): ở 4K context, cache ≈ 1.3GB/user — nhỏ. Ở 128K context, cache ≈ <strong>42.9GB/user</strong> — hơn nửa một H100 80GB, chỉ cho 1 user. Với 10 user ở 32K context, cache đã ~102GB — <strong>gấp gần 3 lần weights INT4 (37GB)</strong>.</p> <p>Kịch bản có thật: deploy Llama 3.3 70B INT4 (~37GB weights) trên 1× H100 80GB, nghĩ còn dư ~43GB cho cache. Với 4 user @ 32K, cache đã ngốn ~43GB — vừa khít. Thêm 1 user là <strong>OOM crash</strong>.</p> <blockquote class="block-tip"> <h5 id="đào-sâu-công-thức">Đào sâu công thức</h5> <p>Mình có bài riêng đi sâu vào công thức KV cache, ví dụ tính tay theo từng model, và cách giảm 50% bằng FP8 quantization: <a href="/synthesis/2026/kv-cache-deep-dive/">KV Cache trong production</a>. Bài này chỉ tóm tắt phần cần cho quyết định self-host vs API.</p> </blockquote> <p>Hai cạm bẫy VRAM khác cần nhớ: <strong>MoE = toàn bộ tham số phải nằm VRAM</strong> (Kimi K2.6 chỉ kích hoạt 32B/token nhưng cả 1T tham số phải sẵn trên GPU, vì token nào cũng có thể route đến bất kỳ expert nào); và <strong>băng thông quan trọng hơn dung lượng</strong> — tốc độ sinh token phụ thuộc memory bandwidth (H100 ~90 token/s cho 70B INT4, DDR4 CPU offload chỉ ~1.3 token/s — đủ để test, không đủ production).</p> <h3 id="kinh-tế-khi-nào-tự-host-mới-rẻ-hơn">Kinh tế: khi nào tự host mới rẻ hơn</h3> <p>Ở mức 1M token/ngày, một case study ghi nhận self-host trên cloud GPU đắt hơn API tới <strong>733 lần</strong> — vì GPU cloud tính tiền theo giờ bất kể bạn dùng bao nhiêu. 4× H100 cloud ~$16–32/giờ ≈ <strong>$11.520/tháng cố định</strong>, dù xử lý 100K hay 10M token/ngày.</p> <table> <thead> <tr> <th style="text-align: right">Token/ngày</th> <th style="text-align: right">Claude Sonnet ($3/$15 mỗi triệu)</th> <th style="text-align: right">Self-host 4×H100</th> <th style="text-align: left">Kết luận</th> </tr> </thead> <tbody> <tr> <td style="text-align: right">100K</td> <td style="text-align: right">~$14/tháng</td> <td style="text-align: right">~$11.520/tháng</td> <td style="text-align: left">API rẻ hơn ~800×</td> </tr> <tr> <td style="text-align: right">1M</td> <td style="text-align: right">~$135/tháng</td> <td style="text-align: right">~$11.520/tháng</td> <td style="text-align: left">API rẻ hơn ~85×</td> </tr> <tr> <td style="text-align: right">50M</td> <td style="text-align: right">~$6.750/tháng</td> <td style="text-align: right">~$11.520/tháng</td> <td style="text-align: left">Hoà vốn</td> </tr> <tr> <td style="text-align: right">100M</td> <td style="text-align: right">~$13.500/tháng</td> <td style="text-align: right">~$11.520/tháng</td> <td style="text-align: left">Self-host thắng</td> </tr> <tr> <td style="text-align: right">500M</td> <td style="text-align: right">~$67.500/tháng</td> <td style="text-align: right">~$11.520/tháng</td> <td style="text-align: left">Self-host thắng rõ</td> </tr> </tbody> </table> <p>Break-even đổi tuỳ so với ai: vs Claude Opus ~2–5M token/ngày; vs Claude Sonnet ~10–20M; vs API giá rẻ (DS V4 Flash $0.14/$0.28 mỗi triệu) ~500M+ — gần như bất khả thi trên 1 cluster. Những gì tốn ~$500/tháng năm ngoái, năm nay tốn ~$50 — “cost collapse” của API đang bào mòn dần lý do self-host.</p> <h3 id="quyết-định-5-tình-huống-nên-tự-host-95-còn-lại--dùng-api">Quyết định: 5 tình huống nên tự host (95% còn lại — dùng API)</h3> <p>Kết luận từ phân tích 500+ AI deployment thực tế: <strong>95% trường hợp nên dùng API.</strong> Chỉ self-host nếu rơi vào 1 trong 5 tình huống:</p> <ol> <li><strong>Lưu lượng cực lớn, ổn định (&gt;50M token/ngày)</strong> và giữ được GPU utilization &gt;70%.</li> <li><strong>Data residency bắt buộc</strong> — HIPAA, SOC 2, air-gapped, hoặc Nghị định 13/2023 với dữ liệu người Việt.</li> <li><strong>Fine-tuned model trên dữ liệu nội bộ</strong> không thể/không muốn đưa lên API người khác.</li> <li><strong>Bulk task đều đặn</strong> (phân loại, embedding, extraction) — workload đều, complexity thấp.</li> <li><strong>Model/version cụ thể không có trên API</strong> — frozen version cho legacy pipeline, hoặc cần kiểm soát inference pipeline hoàn toàn.</li> </ol> <p><strong>Không thuộc 5 trường hợp trên? Dùng API.</strong> Tiết kiệm hàng tuần engineering và ngủ ngon hơn.</p> <h3 id="nếu-tự-host-ship-cho-đúng">Nếu tự host: ship cho đúng</h3> <p>Chọn engine theo mục đích: <strong>vLLM</strong> cho production nhiều user (PagedAttention tăng GPU utilization 24%→98.5%); <strong>SGLang</strong> cho MoE lớn/structured output; <strong>llama.cpp</strong>/<strong>Ollama</strong> cho dev/prototype — <strong>đừng dùng Ollama cho production</strong> (thiếu continuous batching thật).</p> <p>Model chạy được mới là lớp đầu tiên trong 4 lớp cần thiết:</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>┌─────────────────────────────────────────┐
│  L4: GIAO DIỆN — Chat UI, API gateway   │
├─────────────────────────────────────────┤
│  L3: RAG &amp; KNOWLEDGE — Vector DB, Search │
├─────────────────────────────────────────┤
│  L2: BẢO MẬT — SSO, RBAC, ACL, PII mask  │
├─────────────────────────────────────────┤
│  L1: INFERENCE — vLLM/SGLang + GPU + Mon │
└─────────────────────────────────────────┘
</code></pre></div></div> <p>Lộ trình thực tế: Bước 0 — prototype bằng API free tier ($0, 1 ngày); Bước 1 — production v1 bằng API rẻ (~$5–50/tháng, 1 tuần); Bước 2 — nếu cần data residency, 1× RTX 4090 + model nhỏ + vLLM (~$1.600 + setup, 2 tuần); Bước 3 — khi volume &gt;10M token/ngày, đánh giá cluster 4× H100 (1 tháng). Đa số team production 2026 không chọn “hết self-host” hay “hết API”, mà <strong>hybrid</strong>: self-host model rẻ cho bulk task, API frontier cho task khó, qua một router đơn giản.</p> <h3 id="cạm-bẫy--điều-học-được">Cạm bẫy &amp; điều học được</h3> <p>Đây là phần đáng đọc kỹ nhất — kinh nghiệm thật, không có trong docs chính thức.</p> <p>Bảng break-even ở trên còn <em>lạc quan</em> vì chưa tính các khoản này:</p> <table> <thead> <tr> <th style="text-align: left">Hạng mục</th> <th style="text-align: left">Chi phí thực tế</th> </tr> </thead> <tbody> <tr> <td style="text-align: left">GPU idle</td> <td style="text-align: left">GPU ở 10% load → cost per-token tăng <strong>10×</strong></td> </tr> <tr> <td style="text-align: left">Setup</td> <td style="text-align: left">2–4 tuần senior ML engineer</td> </tr> <tr> <td style="text-align: left">Bảo trì</td> <td style="text-align: left">8–20 giờ/tháng engineering time</td> </tr> <tr> <td style="text-align: left">Điện + cooling</td> <td style="text-align: left">$500–4.000/tháng cho cụm 4–8× H100</td> </tr> </tbody> </table> <blockquote class="block-danger"> <h5 id="case-study-đáng-nhớ">Case study đáng nhớ</h5> <p>Một công ty tự host trên 4× A10G, tốn $5.175/tháng. API tương đương chỉ $1.870/tháng. Họ trả <strong>2.8× nhiều hơn</strong> cho “giải pháp tiết kiệm”.</p> </blockquote> <p><strong>Những gì mình cân nhắc nhưng bỏ — kèm lý do:</strong></p> <ul> <li><strong>Self-host toàn bộ để độc lập API</strong> — cân nhắc vì lo phụ thuộc vendor, nhưng bỏ: economics chỉ thắng rõ ở &gt;50M token/ngày, phần lớn sản phẩm chưa chạm ngưỡng đó, và rủi ro vận hành (OOM, incident 2 giờ sáng) không đáng đổi lấy sự độc lập chưa cần thiết.</li> <li><strong>Ollama cho production vì dễ setup</strong> — cân nhắc vì team chưa quen vLLM, nhưng bỏ: thiếu PagedAttention/continuous batching thật, throughput không đủ khi nhiều user vào cùng lúc. Dùng Ollama cho demo, chuyển vLLM trước khi có user thật.</li> <li><strong>Bỏ qua data residency vì “chắc không ai kiểm tra”</strong> — cân nhắc vì self-host tốn thời gian, nhưng bỏ: rủi ro pháp lý theo Nghị định 13/2023 (hoặc HIPAA/SOC2 tuỳ ngành) không đáng đánh đổi, đặc biệt khi chi phí tuân thủ về sau luôn cao hơn chi phí làm đúng từ đầu.</li> <li><strong>GPU cloud on-demand thay vì reserved</strong> — cân nhắc để linh hoạt, nhưng bỏ nếu traffic không đều: GPU idle 10% load đã đẩy cost/token tăng 10×, on-demand không tiết kiệm như tưởng nếu không giữ utilization cao liên tục.</li> </ul> <h3 id="kết">Kết</h3> <p>Ba câu hỏi trước khi quyết: (1) Bạn xử lý bao nhiêu token/ngày — dưới 10M thì API, trên 50M mới cân nhắc self-host; (2) Dữ liệu có phải air-gap không — có thì self-host bắt buộc, không thì API an toàn hơn (ít gánh nặng vận hành); (3) Team có MLOps không — không thì API, có thì tính ROI 12 tháng rồi quyết.</p> <p>Đừng self-host vì “nghe tưởng rẻ hơn”. Hãy self-host vì <strong>con số cho thấy nó rẻ hơn</strong> — sau khi đã tính đủ idle cost, engineering time, và cơ hội bỏ lỡ. Nếu quyết định tự host, xem thêm bài <a href="/synthesis/2026/kv-cache-deep-dive/">KV Cache trong production</a> để tính đúng VRAM trước khi deploy. Có số liệu mới hơn hay góp ý, rất mong bạn để lại ở phần bình luận.</p> <hr/> <h2 id="english">English</h2> <p>In May 2026, the benchmark gap between the best open-weight model (Kimi K2.6, SWE-bench 80.2%) and the best closed model (Claude Opus 4.6, 80.8%) shrank to <strong>0.6%</strong>, while open-weight API pricing runs 5–70× cheaper. The question “should we self-host our LLM?” suddenly became real — and I’ve noticed most teams get the math wrong in one of two directions: either they undersize the GPU and hit OOM the moment real users show up, or they assume self-hosting is cheap and end up paying more than the API by month’s end.</p> <blockquote class="block-tip"> <h5 id="tldr-1">TL;DR</h5> <p><strong>95% of cases should use an API.</strong> Self-hosting only pays off at very high volume (&gt;50M tokens/day) or when data residency is mandatory. Real VRAM = Weights + KV Cache × concurrent users + overhead — forgetting KV cache is a guaranteed OOM after deploy. The break-even point is high (~50–100M tokens/day vs. expensive frontier APIs), and <strong>hidden costs</strong> (idle GPU time, setup, maintenance) are what actually kill ROI — not the GPU price tag.</p> </blockquote> <p><em>Sources compiled from the Onyx AI Leaderboard, official model cards, and real-world cost breakdowns. Full list at the <a href="#nguồn-tham-khảo">end of the post</a>.</em></p> <hr/> <h3 id="context--constraints">Context &amp; Constraints</h3> <p>This post is for you if your team is <strong>weighing self-hosting vs. an API for a production LLM deployment</strong>, you already have a candidate model, and you’re unsure which way to go. If you’re still prototyping, the answer is almost always “use an API” — jump to the Conclusion to confirm.</p> <p>Before doing any math, nail down these 5 constraints — they change the answer by orders of magnitude:</p> <ul> <li><strong>Expected volume (tokens/day):</strong> the single most important variable, it determines your break-even point. Under 10M/day, an API almost always wins.</li> <li><strong>Budget shape:</strong> capex (buying GPUs) vs. opex (renting cloud, paying monthly) — self-hosting requires capital commitment or a long-term contract.</li> <li><strong>Compliance/data residency requirements:</strong> HIPAA (US healthcare), SOC 2 (finance), air-gapped (defense), or region-specific personal data protection laws.</li> <li><strong>Your team’s MLOps capacity:</strong> who operates, tunes, and handles a 2am incident? Without this person, self-hosting is an operational risk, not a saving.</li> <li><strong>Deadline:</strong> self-hosting needs a minimum of 1–4 weeks setup; an API can ship the same day.</li> </ul> <h3 id="the-2026-landscape-briefly">The 2026 Landscape (Briefly)</h3> <p>Open-weight models moved fast in early 2026 — at least 4 major upgrades in Q1–Q2 alone (Kimi K2.5→K2.6, DeepSeek V3.2→V4, GLM-5→5.1, Qwen 3.5→3.6). If you’re reading a model comparison without a date stamp, assume it’s stale.</p> <table> <thead> <tr> <th style="text-align: left">Model</th> <th style="text-align: right">Active/token</th> <th style="text-align: right">Context</th> <th style="text-align: left">License</th> <th style="text-align: right">SWE-bench</th> </tr> </thead> <tbody> <tr> <td style="text-align: left">DeepSeek V4 Pro</td> <td style="text-align: right">49B/1.6T</td> <td style="text-align: right"><strong>1M</strong></td> <td style="text-align: left">MIT</td> <td style="text-align: right"><strong>80.6%</strong></td> </tr> <tr> <td style="text-align: left">Kimi K2.6</td> <td style="text-align: right">32B/1T</td> <td style="text-align: right">262K</td> <td style="text-align: left">MIT+</td> <td style="text-align: right">80.2%</td> </tr> <tr> <td style="text-align: left">GLM-5.1</td> <td style="text-align: right">40B/744B</td> <td style="text-align: right">200K</td> <td style="text-align: left">MIT</td> <td style="text-align: right">—</td> </tr> <tr> <td style="text-align: left">Qwen 3.5</td> <td style="text-align: right">17B/397B</td> <td style="text-align: right">—</td> <td style="text-align: left">Apache</td> <td style="text-align: right">—</td> </tr> <tr> <td style="text-align: left">Llama 4 Scout</td> <td style="text-align: right">17B/109B</td> <td style="text-align: right"><strong>10M</strong></td> <td style="text-align: left">Llama 4</td> <td style="text-align: right">—</td> </tr> <tr> <td style="text-align: left">Claude Opus 4.6*</td> <td style="text-align: right">—</td> <td style="text-align: right">—</td> <td style="text-align: left">—</td> <td style="text-align: right">80.8%</td> </tr> </tbody> </table> <p><em>* Closed model, shown for reference.</em></p> <p>Three takeaways: most frontier models are <strong>MoE</strong>, so you can’t compare models by parameter count alone; context windows are exploding, but KV cache scales linearly with tokens so production deployments are usually capped around 128K–256K in practice; and <strong>MIT licensing dominates</strong> — free to commercialize, fine-tune, and redistribute. Benchmarks are also harness-dependent (the same model can swing 10 points depending on the harness) — always test on your own workload; there’s no universally “best” model.</p> <h3 id="the-vram-formula-the-number-everyone-gets-wrong">The VRAM Formula: The Number Everyone Gets Wrong</h3> \[\text{VRAM MIN} = \text{Weights} + \text{KV Cache} + \text{Activations} + \text{Overhead}\] <p>Weights are the part everyone sizes correctly (FP16 = 2 bytes/param, INT4 = 0.5 bytes/param). <strong>KV cache</strong> is the silent culprit — every concurrent user needs its own cache, and it scales linearly with <code class="language-plaintext highlighter-rouge">context length × users</code>.</p> <p>For Llama 3.3 70B (80 layers, 8 KV heads, BF16): at 4K context, cache is only ≈1.3GB/user. At 128K context, it balloons to <strong>≈42.9GB/user</strong> — over half an H100 80GB, for a single user. At 10 users and 32K context, cache alone is ~102GB — <strong>almost 3× the INT4 weights (37GB)</strong>.</p> <p>A real scenario: deploying Llama 3.3 70B INT4 (~37GB weights) on a single H100 80GB, assuming ~43GB is left over for cache. At 4 concurrent users @ 32K context, cache already eats that ~43GB — a perfect fit with zero headroom. One more user and you get an <strong>OOM crash</strong>.</p> <blockquote class="block-tip"> <h5 id="going-deeper-on-the-formula">Going deeper on the formula</h5> <p>I have a dedicated post working through the full KV cache formula, hand-calculated examples per model, and how to cut memory 50% with FP8 quantization: <a href="/synthesis/2026/kv-cache-deep-dive/">KV Cache in Production</a>. This post only covers what you need for the self-host-vs-API decision.</p> </blockquote> <p>Two more VRAM traps worth knowing: <strong>MoE means all parameters must live in VRAM</strong> (Kimi K2.6 only activates 32B/token, but the full 1T parameters must sit on the GPU, since any token could route to any expert); and <strong>bandwidth matters more than capacity</strong> — token generation speed depends on memory bandwidth (H100 gets ~90 tokens/sec on a 70B INT4 model, while DDR4 CPU offload only manages ~1.3 tokens/sec — fine for testing, not for production).</p> <h3 id="economics-when-self-hosting-actually-pays-off">Economics: When Self-Hosting Actually Pays Off</h3> <p>At 1M tokens/day, one case study found self-hosting on cloud GPUs cost <strong>733× more</strong> than an API — because cloud GPUs bill hourly regardless of utilization. 4× H100 on cloud at ~$16–32/hour comes out to a <strong>fixed ~$11,520/month</strong>, whether you process 100K or 10M tokens a day.</p> <table> <thead> <tr> <th style="text-align: right">Tokens/day</th> <th style="text-align: right">Claude Sonnet ($3/$15 per million)</th> <th style="text-align: right">Self-host 4×H100</th> <th style="text-align: left">Verdict</th> </tr> </thead> <tbody> <tr> <td style="text-align: right">100K</td> <td style="text-align: right">~$14/month</td> <td style="text-align: right">~$11,520/month</td> <td style="text-align: left">API ~800× cheaper</td> </tr> <tr> <td style="text-align: right">1M</td> <td style="text-align: right">~$135/month</td> <td style="text-align: right">~$11,520/month</td> <td style="text-align: left">API ~85× cheaper</td> </tr> <tr> <td style="text-align: right">50M</td> <td style="text-align: right">~$6,750/month</td> <td style="text-align: right">~$11,520/month</td> <td style="text-align: left">Roughly break-even</td> </tr> <tr> <td style="text-align: right">100M</td> <td style="text-align: right">~$13,500/month</td> <td style="text-align: right">~$11,520/month</td> <td style="text-align: left">Self-host wins</td> </tr> <tr> <td style="text-align: right">500M</td> <td style="text-align: right">~$67,500/month</td> <td style="text-align: right">~$11,520/month</td> <td style="text-align: left">Self-host wins clearly</td> </tr> </tbody> </table> <p>Break-even shifts depending on your comparison: vs. Claude Opus, ~2–5M tokens/day; vs. Claude Sonnet, ~10–20M; vs. cheap APIs (DS V4 Flash at $0.14/$0.28 per million), ~500M+ — nearly unreachable on a single cluster. What cost ~$500/month a year ago now costs ~$50 — the API “cost collapse” keeps eroding the case for self-hosting.</p> <h3 id="the-decision-5-situations-worth-self-hosting-the-other-95-should-use-an-api">The Decision: 5 Situations Worth Self-Hosting (the Other 95% Should Use an API)</h3> <p>Drawn from analyzing 500+ real AI deployments: <strong>95% of cases should use an API.</strong> Only self-host if you fall into one of these 5 situations:</p> <ol> <li><strong>Very high, sustained traffic (&gt;50M tokens/day)</strong> while maintaining &gt;70% GPU utilization.</li> <li><strong>Mandatory data residency</strong> — HIPAA, SOC 2, air-gapped environments, or region-specific personal data laws.</li> <li><strong>A fine-tuned model on proprietary data</strong> you can’t or won’t put on a third-party API.</li> <li><strong>Steady bulk workloads</strong> (classification, embeddings, extraction) — uniform load, low complexity.</li> <li><strong>A specific model/version unavailable via any API</strong> — a frozen version for a legacy pipeline, or a need for full control over the inference pipeline.</li> </ol> <p><strong>Don’t fall into any of these 5? Use an API.</strong> You’ll save weeks of engineering time and sleep better.</p> <h3 id="if-you-do-self-host-ship-it-right">If You Do Self-Host: Ship It Right</h3> <p>Pick your engine by purpose: <strong>vLLM</strong> for multi-user production (PagedAttention lifts GPU utilization from 24%→98.5%); <strong>SGLang</strong> for large MoE models or structured output; <strong>llama.cpp</strong>/<strong>Ollama</strong> for dev/prototyping only — <strong>don’t use Ollama in production</strong> (it lacks real continuous batching).</p> <p>Getting the model running is only layer one of four:</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>┌─────────────────────────────────────────┐
│  L4: INTERFACE — Chat UI, API gateway    │
├─────────────────────────────────────────┤
│  L3: RAG &amp; KNOWLEDGE — Vector DB, Search │
├─────────────────────────────────────────┤
│  L2: SECURITY — SSO, RBAC, ACL, PII mask │
├─────────────────────────────────────────┤
│  L1: INFERENCE — vLLM/SGLang + GPU + Mon │
└─────────────────────────────────────────┘
</code></pre></div></div> <p>A realistic rollout: Step 0 — prototype on a free-tier API ($0, 1 day); Step 1 — ship production v1 on a cheap API (~$5–50/month, 1 week); Step 2 — if data residency is required, 1× RTX 4090 + a small model + vLLM (~$1,600 + setup, 2 weeks); Step 3 — once volume passes 10M tokens/day, evaluate a 4× H100 cluster (1 month). Most 2026 production systems don’t choose “all self-hosted” or “all API” — they run <strong>hybrid</strong>: cheap self-hosted models for bulk tasks, frontier APIs for hard tasks, routed through a simple dispatcher.</p> <h3 id="pitfalls--lessons-learned">Pitfalls &amp; Lessons Learned</h3> <p>This is the section worth reading most carefully — real experience you won’t find in official docs.</p> <p>The break-even table above is still <em>optimistic</em> — it excludes these costs:</p> <table> <thead> <tr> <th style="text-align: left">Item</th> <th style="text-align: left">Real cost</th> </tr> </thead> <tbody> <tr> <td style="text-align: left">GPU idle time</td> <td style="text-align: left">GPU at 10% load → per-token cost jumps <strong>10×</strong></td> </tr> <tr> <td style="text-align: left">Setup</td> <td style="text-align: left">2–4 weeks of senior ML engineer time</td> </tr> <tr> <td style="text-align: left">Maintenance</td> <td style="text-align: left">8–20 hours/month of engineering time</td> </tr> <tr> <td style="text-align: left">Power + cooling</td> <td style="text-align: left">$500–4,000/month for a 4–8× H100 cluster</td> </tr> </tbody> </table> <blockquote class="block-danger"> <h5 id="a-case-study-worth-remembering">A case study worth remembering</h5> <p>One company self-hosted on 4× A10G at $5,175/month. The equivalent API cost only $1,870/month. They paid <strong>2.8× more</strong> for their “cost-saving” solution.</p> </blockquote> <p><strong>What I considered and rejected — with reasons:</strong></p> <ul> <li><strong>Self-hosting everything to avoid API dependency</strong> — considered out of vendor-lock-in concerns, but rejected: the economics only clearly win above ~50M tokens/day, most products never reach that threshold, and the operational risk (OOM incidents, 2am pages) isn’t worth trading for an independence you don’t yet need.</li> <li><strong>Ollama in production because setup is easy</strong> — considered because the team wasn’t familiar with vLLM, but rejected: it lacks real PagedAttention/continuous batching, so throughput falls short once multiple users hit it concurrently. Use Ollama for demos, switch to vLLM before real users arrive.</li> <li><strong>Skipping data residency because “no one will check”</strong> — considered to save setup time, but rejected: the legal exposure (regional data protection law, or HIPAA/SOC 2 depending on industry) isn’t worth it — compliance costs later are always higher than doing it right from the start.</li> <li><strong>On-demand cloud GPUs instead of reserved</strong> — considered for flexibility, but rejected for uneven traffic: GPU at 10% utilization already pushes per-token cost up 10×, so on-demand doesn’t save as much as it seems unless you can keep utilization consistently high.</li> </ul> <h3 id="conclusion">Conclusion</h3> <p>Three questions before you decide: (1) How many tokens/day are you processing — under 10M means API, over 50M means self-hosting is worth considering; (2) Is your data required to be air-gapped — yes means self-hosting is mandatory, no means an API is safer (less operational burden); (3) Does your team have MLOps capacity — no means API, yes means run a 12-month ROI calculation before deciding.</p> <p>Don’t self-host because it “sounds cheaper.” Self-host because <strong>the numbers show it’s cheaper</strong> — after fully accounting for idle cost, engineering time, and opportunity cost. If you do decide to self-host, see my companion post on <a href="/synthesis/2026/kv-cache-deep-dive/">KV Cache in Production</a> to size VRAM correctly before you deploy. If you have more recent numbers or a different take, I’d genuinely welcome it in the comments.</p> <hr/> <h2 id="nguồn-tham-khảo">Nguồn tham khảo</h2> <p><em>Dùng chung cho cả 2 bản / Shared reference list for both language versions.</em></p> <ol> <li><a href="https://onyx.app/self-hosted-llm-leaderboard">Onyx AI — Self-Hosted LLM Leaderboard</a></li> <li><a href="https://benchlm.ai/blog/posts/best-chinese-llm">BenchLM — Best Chinese LLMs 2026</a></li> <li><a href="https://huggingface.co/deepseek-ai/DeepSeek-V4-Pro">Hugging Face — DeepSeek V4 Pro Model Card</a></li> <li><a href="https://www.spheron.network/blog/gpu-memory-requirements-llm/">Spheron — GPU Memory Requirements for LLMs</a></li> <li><a href="https://dev.to/bytecalculators/the-math-behind-local-llms-how-to-calculate-exact-vram-requirements-before-you-crash-your-gpu-12n5">DEV.to — The Math Behind Local LLMs VRAM</a></li> <li><a href="https://www.braincuber.com/blog/self-hosted-llms-vs-api-based-llms-cost-performance-analysis">Braincuber — Self-Hosted vs API LLMs: Real Cost Breakdown 2026</a></li> <li><a href="https://www.sitepoint.com/self-hosted-llm-costs-2026/">SitePoint — Self-Hosted LLM Costs 2026</a></li> <li><a href="https://devtk.ai/en/blog/self-hosting-llm-vs-api-cost-2026/">DevTk.AI — Self-Host LLM vs API Cost 2026</a></li> <li><a href="https://tokenmix.ai/blog/self-host-llm-vs-api">TokenMix — Self-Host LLM vs API Break-Even</a></li> <li><a href="https://pecollective.com/blog/llm-api-pricing-comparison/">PE Collective — LLM API Pricing 2026</a></li> <li><a href="https://www.marktechpost.com/2026/04/20/moonshot-ai-releases-kimi-k2-6-with-long-horizon-coding-agent-swarm-scaling-to-300-sub-agents-and-4000-coordinated-steps/">MarkTechPost — Kimi K2.6 Release</a></li> <li><a href="https://www.aimadetools.com/blog/glm-5-1-vs-deepseek-vs-qwen-coding/">AIMadeTools — GLM-5.1 vs DeepSeek vs Qwen Coding</a></li> <li><a href="https://lmcache.ai/kv_cache_calculator.html">LMCache — KV Cache Size Calculator</a></li> <li><a href="https://localllm.in/blog/llamacpp-vram-requirements-for-local-llms">LocalLLM — llama.cpp VRAM Requirements Guide</a></li> <li><a href="https://renezander.com/guides/self-hosted-llm-vs-api/">Rene Zander — Self-Hosted LLM vs API Break-Even 2026</a></li> </ol> <hr/> <p><em>Cập nhật / Updated: 24/07/2026. Mọi con số có thể thay đổi nhanh — verify trước khi ra quyết định ngân sách. / Numbers move fast in this space — verify before making a budget decision.</em></p>]]></content><author><name></name></author><category term="AI"/><category term="infrastructure"/><category term="self-host"/><category term="production"/><summary type="html"><![CDATA[Vì sao 95% trường hợp nên dùng API, cách tính đúng VRAM và điểm hoà vốn, và stack production nếu bạn quyết tự host. Bài song ngữ Việt/Anh, số liệu 05/2026. — Why 95% of teams should use an API, how to calculate real VRAM and break-even, and the production stack if you do self-host. Bilingual VI/EN, data as of May 2026.]]></summary></entry></feed>